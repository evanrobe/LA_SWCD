# Base URL for the LASWCD.WebApi backend.
# Leave unset in development — the Vite dev server proxies /api to http://localhost:5240 (see vite.config.ts).
# Set this for production builds where the SPA is not served behind the same proxy.
VITE_API_BASE_URL=
